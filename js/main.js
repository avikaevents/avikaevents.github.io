import { get, _urls } from './constant.js';

const route_name=window.location.pathname;

const fetch_data=async () => {
    // const res=await fetch(_urls._sheets_url);
    // let data= await res.clone().json();
    // let el = document.getElementById("sachinduhan");
    // el.innerHTML += data;
    // console.log(data);
};

fetch_data();